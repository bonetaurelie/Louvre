{% extends 'ABCoreBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - {{ 'paiement.titre' |trans }}
{% endblock %}

{% block body %}

    <div class="payment_block">
    <div class="payment">
        <h1 class="text-center">{{ 'choix.paiement' | trans }}</h1>
        <p class="montant col-lg-6">Le montant est de : {{ val_commande.tarif }}€</p>
    </div>

        {% for message in app.session.flashbag.get('error') %}
            <div class="alert alert-danger alert-bold-border square fade in alert-dismissable">
                <i class="fa fa-check"></i>
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                {{ message }}
            </div>
        {% endfor %}



    <div class="bouton">
    <div class="stripe col-xs-offset-2 col-sm-offset-3 col-md-offset-4">
        <form action="{{ path('ab_core_stripe', {'id': val_commande.id}) }}" method="POST">
            <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                    data-key="pk_test_NHnOC4NL0JVZGhMuDumzLF0x"
                    data-image="{{ asset('bundles/abcore/images/le-musee-du-louvre.jpg') }}"
                    data-amount="{{ stripe_montant }}"
                    data-address='true'
                    data-currency='eur'
                    data-name='Musée du Louvre'
                    data-description='Montant de la transaction'
                    data-panel-label='Valider'
            ></script>
        </form>
    </div>

    <div class="paypal col-xs-offset-5 col-sm-offset-5 col-md-offset-6">
        <form action="https://www.sandbox.paypal.com/cgi-bin/webscr" method="post" target="_top">
            <input type="hidden" name="cmd" value="_s-xclick">
            <input type="hidden" name="hosted_button_id" value="AALNLW46AZ6XE">
            <input type="hidden" name="currency_code" value="EUR">
            <input type="hidden" name="item_name" value="Billet pour la visite du Louvre">
            <input type="hidden" name="amount" value="16">
            <input type="image" src="https://www.sandbox.paypal.com/fr_FR/FR/i/btn/btn_paynowCC_LG.gif" border="0" name="submit" alt="PayPal, le réflexe sécurité pour payer en ligne">
            <img alt="" border="0" src="https://www.sandbox.paypal.com/fr_FR/i/scr/pixel.gif" width="1" height="1">
        </form>
    </div>
    </div>
        <div class="annulation col-xs-offset-8 col-sm-offset-7 col-md-offset-8">
            <a class="btn btn-danger" href="{{ path('ab_core_accueil')}}" role="button">{{ 'annulation' |trans }}</a>
        </div>
    </div>
{% endblock %}