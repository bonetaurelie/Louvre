# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    #Service Stripe
    ab.stripe_client:
        class: AB\CoreBundle\Services\StripeClientService
        arguments: ['%stripe_secret_key%', "@doctrine.orm.entity_manager"]
    ab.val_commande:
        class: AB\CoreBundle\Services\ValidationCommandeService
        arguments: ["@doctrine.orm.entity_manager"]